export default function FormLogin({onCick}) {
    return (
        <article>
            <div>

            </div>

            <div>

            </div>

            <div>
                <p onClick={onCick}> have a account? <a href="#">Login</a></p>
            </div>
        </article>
    )
}